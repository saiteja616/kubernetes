---
apiVersion: v1
kind: Service
metadata:
  name: loadbalancer-service
  labels:
    name: saiteja
spec:
  type: LoadBalancer
  ports:
    - targetPort: 80
      port: 80
      nodePort: 30009
  selector: 
    author: saiteja
    type: proxy
...

